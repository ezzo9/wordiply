# Copy to .env.local for local development, or set in Vercel project settings.

# GA4 measurement ID (e.g. G-XXXXXXXXXX). Leave unset to keep analytics
# disabled — components/Analytics.tsx renders nothing without it.
NEXT_PUBLIC_GA_MEASUREMENT_ID=
